object Main {
  def main(args: Array[String]) {
    /**
     * TODO -
     * A tool that allows the user to enter a text string and then in a separate control enter a regex pattern. It will
     * run the regular expression against the source text and return any matches or flag errors in the regular
     * expression.
     */
  }
}