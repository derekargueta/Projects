package Numbers

/**
 * Created by derek on 7/9/15.
 */
object Main {
  def main(args: Array[String]) {
    /**
     * TODO -
     *
     * Ask the user to enter f(x) and the limit value,
     * then return the value of the limit statement Optional: Make the
     * calculator capable of supporting infinite limits.
     */
  }
}
