object Main {
  def main(args: Array[String]) {
    /**
     * TODO -
     * Enter an IP address and a port range where the program will then attempt to find open ports on the given computer
     * by connecting to each of them. On any successful connections mark the port as open.
     */
  }
}