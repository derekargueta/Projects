object Main {
  def main(args: Array[String]) {
    /**
     * TODO -
     * Create an application which connects to a site and pulls out all links, or images, and saves them to a list.
     * Optional: Organize the indexed content and don’t allow duplicates. Have it put the results into an easily
     * searchable index file.
     */
  }
}