## Usage (assuming you have go installed)
Build the application using
```
go build -o ../bin/collatz main.go
```

Run the application with 
```
../bin/collatz #
```

It expects a integer larger than 1 as a command-line argument